import React from 'react';

/**
 * AdBanner Slot Component
 * Currently disabled / collapsed to zero dimensions per user request.
 * Can be re-enabled with genuine Google AdSense <ins className="adsbygoogle" ... /> tags once approved.
 */
export default function AdBanner() {
  return null;
}
